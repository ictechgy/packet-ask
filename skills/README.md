스킬 원문은 `src/packet_ask/data/SKILL.md` 이다.

```bash
packet-ask install-skills
```

가 `~/.claude/skills/packet-ask`, `~/.grok/skills/packet-ask`, `~/.codex/skills/packet-ask`, `~/.agents/skills/packet-ask` 에 설치한다.
